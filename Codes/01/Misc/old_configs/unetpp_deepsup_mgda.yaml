inherits: default.yaml

model:
  architecture: "unetpp"
  deep_supervision: true

loss:
  strategy: "mgda"
  deep_supervision:
    enabled: true
    weights: [0.5, 0.3, 0.2, 0.1]

mgda:
  enabled: true
